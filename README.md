# Simple Git Workflow Demo

This repository is a tiny text-only sample project used to demonstrate a realistic Git workflow with feature branches, merge commits, and a hotfix branch.

The project keeps simple notes for tasks, users, and configuration so the commit history stays easy to read.

This README hotfix clarifies that the repository is intentionally minimal.
